import { NextRequest, NextResponse } from 'next/server';
import { setUserStatus } from '@/lib/database';
import { resolveViewer } from '@/lib/viewer';
import { correlationId } from '@/lib/security';
import { isSameOrigin } from '@/lib/auth';
export async function POST(request: NextRequest) { const requestId=correlationId(); if(!isSameOrigin(request)) return NextResponse.json({code:'FORBIDDEN',error:'Permintaan tidak diizinkan.',correlation_id:requestId},{status:403}); const viewer=await resolveViewer(request.nextUrl.searchParams.get('view')??undefined); if(!viewer||viewer.role!=='admin') return NextResponse.json({code:'FORBIDDEN',error:'Akses khusus Admin.',correlation_id:requestId},{status:403}); const body=await request.json() as {targetSlug?:string;status?:string;purpose?:string}; if(!body.targetSlug||!['active','inactive'].includes(String(body.status))||String(body.purpose??'').trim().length<5) return NextResponse.json({code:'VALIDATION_FAILED',error:'Target, status, dan alasan wajib diisi.',correlation_id:requestId},{status:422}); await setUserStatus(viewer.slug,body.targetSlug,body.status as 'active'|'inactive',String(body.purpose).trim(),requestId); return NextResponse.json({message:'Status user diperbarui.',correlation_id:requestId}); }
